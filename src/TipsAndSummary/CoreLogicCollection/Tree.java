package TipsAndSummary.CoreLogicCollection;

public class Tree {
    /***

     Calculating a property of the subtree: If the helper function's primary job is to compute a value
     (like height, size, sum, count, a boolean flag indicating a certain condition is met)
     for the subtree rooted at its current node, it will typically return that value.


     Building a list of nodes in a specific order
     Modifying node properties
     Printing the tree structure
     */
}
