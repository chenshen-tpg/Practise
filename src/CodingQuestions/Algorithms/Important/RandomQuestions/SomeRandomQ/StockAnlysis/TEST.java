package CodingQuestions.Algorithms.Important.RandomQuestions.SomeRandomQ.StockAnlysis;

public class TEST {
    /***
     implement the method getTopStock(stocks, prices) which takes as input:
     an array of  Strings (stocks), representing the considered stocks.
     an array of 2 dimentions(prices), representing the stock pricess (inner lists)
     for each day

     An example input would be like this:
     stocks = ["AMZN", "CACC", "EQIX", "GOOG", "ORLY", "ULTA"]
     prices = [ [12.81, 11.09, 12.11, 10.93, 9.83, 8.14], [13.00, 11.50, 12.50, 11.00, 10.00, 8.50], ... ]

     Task:
     For each day, determine which stock had the highest price.
     Return an array of strings, where each element is the name of the stock with the highest price for that day.
     Example Output:
     For the above input, the output would be: ["AMZN", "AMZN", ...] (assuming "AMZN" had the highest price on both days).
     */
}
